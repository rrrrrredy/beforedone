# v1.0.0 manual release evidence

This record closes the local/manual release boundary for BeforeDone v1.0.0.
Repository Actions were intentionally disabled before publishing, so this
release does not claim GitHub OIDC build provenance.

## Immutable inputs

- Source commit and public `main` at release time:
  `b0b17adc083e34607d51cffec0f2a37bcdd3f346`.
- Public tag: `v1.0.0`, pointing to the same commit. GitHub Release creation
  produced a lightweight tag; the published tag was not rewritten afterward.
- Published at: `2026-07-21T06:13:06Z`.
- Release: <https://github.com/rrrrrredy/beforedone/releases/tag/v1.0.0>
- Pages source: classic `gh-pages:/`, commit
  `f0a600b0d0c000365482404882476b3c8dbaf586`.

## Local quality and security gates

- Full Go suite passed from runtime source commit `8012cfc`; a path-scoped diff
  confirmed that `go.mod`, `go.sum`, `cmd`, `internal`, `schemas`, and `plugins`
  were unchanged between that commit and the release commit.
- `go mod verify`, distribution validation for version `1.0.0`, workflow lint,
  JavaScript syntax checks, sitemap XML parsing, and a version-injected Windows
  smoke build passed locally.
- Gitleaks v8.30.1 scanned the complete release history: two commits,
  approximately 896,681 bytes, with no leaks found.
- Release tools: Go 1.26.5 windows/amd64, GoReleaser 2.17.0, Syft 1.44.0.
- GitHub vulnerability alerts and Dependabot security updates are enabled.
  The dependency-graph endpoint returns an SPDX 2.3 SBOM. Secret scanning and
  push protection are enabled. GitHub's API left secret validity checks
  disabled; this is a recorded repository-setting exception, not a claimed
  pass.

## Release asset audit

The draft contained 17 uniquely named uploaded assets: six platform archives,
six SPDX SBOMs, `checksums.txt`, Homebrew and Scoop manifests, and the
Skills-only Directory zip plus its independent checksum.

| Archive | SHA-256 |
| --- | --- |
| `beforedone_1.0.0_darwin_amd64.tar.gz` | `0d993acf5f2da892fca3d3376061bf4e621a2dcbf8938ac14d1011fcb7539f63` |
| `beforedone_1.0.0_darwin_arm64.tar.gz` | `d3dba497a2ccee8918c0f81c18a8c31957a8a9a077e3166b4ebce4beef70e66d` |
| `beforedone_1.0.0_linux_amd64.tar.gz` | `c4083dc7cb509bfda1780e8f678ec4dd8fd41eec214296372ce56c362da39194` |
| `beforedone_1.0.0_linux_arm64.tar.gz` | `040d24ac57fd777dbfc47fcd607c72f75f148646a1b73f1c2faf3c5dd1bfee93` |
| `beforedone_1.0.0_windows_amd64.zip` | `9dd6e9c85b9c58601179d217b6ffe3cf269c4dad85df9ed99b2cb804132aa291` |
| `beforedone_1.0.0_windows_arm64.zip` | `70e633130704a8f54891422fa5c29c5e6368593cb64796fda87e8631f7965f3e` |

All 17 draft assets were downloaded into a new directory and matched the local
SHA-256 values byte for byte. After publication, an unauthenticated download of
the Windows amd64 archive returned HTTP 200 and the same digest. Its extracted
binary reported `beforedone 1.0.0` and passed the adapter fixtures.

All six packaged binaries report Go 1.26.5, module version `v1.0.0`,
`CGO_ENABLED=0`, the expected GOOS/GOARCH, release revision `b0b17adc...`, and
`vcs.modified=false`. Every archive contains the binary, `LICENSE`, `NOTICE`,
and `README.md`; all six SBOMs declare SPDX 2.3.

## Public installability audit

- The public Windows amd64 archive was downloaded, extracted, and run on
  Windows x64. `beforedone version` reported `1.0.0`; the public Adapter Kit
  fixtures passed 3/3.
- `go install github.com/rrrrrredy/beforedone/cmd/beforedone@v1.0.0` succeeded
  with GOBIN, GOPATH, module cache, and build cache isolated under a new D-drive
  audit directory. The installed binary reported `1.0.0` and passed the same
  fixtures. The Go proxy currently resolves `@latest` to `v1.0.0`.
- The tagged Marketplace source resolves `plugins/beforedone`; manifest version,
  two Skills, hooks, and Windows/Unix wrappers are present. With the public
  Windows binary on a process-local PATH, the PowerShell Hook persisted a real
  `SessionStart` event and fail-closed a `Stop` event when project configuration
  was missing.
- Both tagged standalone `SKILL.md` URLs return HTTP 200. The public skills.sh
  pages for `verify-before-done` and `investigate-agent-incident` also return
  HTTP 200. The Skills-only Directory bundle contains the two Skills and their
  metadata byte-for-byte after newline normalization, and contains no hooks.

The audit did not write the user's global Codex or Skills directories.

## Public surface audit

- `releases/latest` redirects to `v1.0.0` and returns HTTP 200.
- The release exposes all 17 assets; `checksums.txt` covers the six archives and
  six SBOMs.
- <https://rrrrrredy.github.io/beforedone/> and
  <https://rrrrrredy.github.io/beforedone/guide.html#fullmd> return HTTP 200.
- The guide loads the published `README.md`, and desktop 1440×900 plus mobile
  390×844 browser checks found no horizontal overflow or console errors.
- Repository Actions remain disabled. Publishing the tag and Release started no
  user workflow and consumed no Actions quota. Enabling the dependency graph
  later created one GitHub-managed `dynamic/dependabot/update-graph` record;
  its Actions timing response reports Ubuntu `total_ms: 0` and job
  `duration_ms: 0`.

## Explicitly outside this evidence boundary

This record does not claim native execution of the macOS, Linux, or Windows
arm64 archives. This host has no WSL distribution, Docker/Podman runtime, Linux
user-mode emulator, or macOS environment, so cross-compilation and archive
inspection are not presented as runtime proof. It also does not claim a clean
Codex Marketplace/Skills installation, OpenAI Directory approval, final media,
YouTube publication, or a live Product Hunt listing. Those remain visible in
the launch control room instead of being inferred from build success.
